<UserControl x:Class="AssumptionChecker.VsExtension.AssumptionCheckerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
             Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
             FontFamily="{DynamicResource {x:Static vs:VsFonts.EnvironmentFontFamilyKey}}"
             FontSize="{DynamicResource {x:Static vs:VsFonts.EnvironmentFontSizeKey}}">
    <UserControl.Resources>

      <!-- == ComboBoxItem style: VS-themed background/highlight == -->
      <Style x:Key="VsComboBoxItemStyle" TargetType="ComboBoxItem">
         <Setter Property="Background" Value="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"/>
         <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"/>
         <Setter Property="Padding" Value="4,2"/>
         <Setter Property="Template">
            <Setter.Value>
               <ControlTemplate TargetType="ComboBoxItem">
                  <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                     <ContentPresenter/>
                  </Border>
                  <ControlTemplate.Triggers>
                     <Trigger Property="IsHighlighted" Value="True">
                        <Setter Property="Background" Value="{DynamicResource {x:Static vs:VsBrushes.HighlightKey}}"/>
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.HighlightTextKey}}"/>
                     </Trigger>
                  </ControlTemplate.Triggers>
               </ControlTemplate>
            </Setter.Value>
         </Setter>
      </Style>

      <!-- == ComboBox style: custom template so the popup uses VsBrushes instead of SystemColors == -->
      <Style x:Key="VsComboBoxStyle" TargetType="ComboBox">
         <Setter Property="ItemContainerStyle" Value="{StaticResource VsComboBoxItemStyle}"/>
         <Setter Property="Template">
            <Setter.Value>
               <ControlTemplate TargetType="ComboBox">
                  <Grid>
                     <!-- Toggle button renders the combo box body and arrow -->
                     <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   Focusable="False">
                        <ToggleButton.Template>
                           <ControlTemplate TargetType="ToggleButton">
                              <Border Background="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"
                                      BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBorderKey}}"
                                      BorderThickness="1">
                                 <Grid>
                                    <Grid.ColumnDefinitions>
                                       <ColumnDefinition Width="*"/>
                                       <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <Rectangle Grid.Column="0"/>
                                    <!-- Dropdown arrow -->
                                    <Path Grid.Column="1"
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          Fill="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                 </Grid>
                              </Border>
                           </ControlTemplate>
                        </ToggleButton.Template>
                     </ToggleButton>

                     <!-- Selected item display overlays the toggle button -->
                     <ContentPresenter Margin="5,2,22,2"
                                       Content="{TemplateBinding SelectionBoxItem}"
                                       ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       TextBlock.Foreground="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"/>

                     <!-- Dropdown popup with VS-themed background -->
                     <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                            AllowsTransparency="True"
                            Focusable="False"
                            PopupAnimation="Slide"
                            Placement="Bottom"
                            PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                        <Border Background="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"
                                BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBorderKey}}"
                                BorderThickness="1"
                                MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                           <ScrollViewer CanContentScroll="True"
                                         MaxHeight="{TemplateBinding MaxDropDownHeight}">
                              <ItemsPresenter/>
                           </ScrollViewer>
                        </Border>
                     </Popup>
                  </Grid>
               </ControlTemplate>
            </Setter.Value>
         </Setter>
      </Style>

      <!-- == BoolToVisibility converter for key status indicators == -->
      <BooleanToVisibilityConverter x:Key="BoolToVis"/>

   </UserControl.Resources>

   <DockPanel Margin="8">
        <!-- == Input area == -->
        <TextBlock Text="Enter prompt to analyze:"
                   FontWeight="Bold"
                   Margin="0,0,0,4"
                   Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                   DockPanel.Dock="Top"/>
        <TextBox Text="{Binding PromptText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MinHeight="80"
                 MaxHeight="150"
                 Margin="0,0,0,8"
                 Background="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"
                 Foreground="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"
                 BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBorderKey}}"
                 VerticalScrollBarVisibility="Auto"
                 DockPanel.Dock="Top"
                 PreviewKeyDown="PromptTextBox_KeyDown"/>

        <!-- == Model selector == -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" DockPanel.Dock="Top">
            <TextBlock Text="Model:"
                       VerticalAlignment="Center"
                       Margin="0,0,6,0"
                       Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"/>
            <ComboBox ItemsSource="{Binding AvailableModels}"
                      SelectedItem="{Binding SelectedModel, Mode=TwoWay}"
                      MinWidth="140"
                      Style="{StaticResource VsComboBoxStyle}"/>
        </StackPanel>

        <!-- == Settings expander: API key management == -->
        <Expander Header="API Key Settings"
                  IsExpanded="{Binding SettingsExpanded}"
                  Margin="0,0,0,8"
                  DockPanel.Dock="Top"
                  Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}">
            <Border BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBorderKey}}"
                    BorderThickness="1"
                    Padding="8"
                    Margin="0,4,0,0"
                    Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}">
                <StackPanel>
                    <!-- OpenAI key row -->
                    <TextBlock Text="OpenAI API Key:"
                               Margin="0,0,0,2"
                               Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <PasswordBox x:Name="OpenAiKeyBox"
                                     Width="220"
                                     PasswordChanged="OpenAiKeyBox_PasswordChanged"
                                     Background="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"
                                     Foreground="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"
                                     BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBorderKey}}"/>
                        <Button Command="{Binding SaveOpenAiKeyCommand}"
                                Content="Save"
                                Padding="8,2"
                                Margin="6,0,0,0"
                                Background="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"
                                Foreground="{DynamicResource {x:Static vs:VsBrushes.ButtonTextKey}}"
                                BorderBrush="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"/>
                    </StackPanel>

                    <!-- Anthropic key row -->
                    <TextBlock Text="Anthropic API Key:"
                               Margin="0,0,0,2"
                               Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <PasswordBox x:Name="AnthropicKeyBox"
                                     Width="220"
                                     PasswordChanged="AnthropicKeyBox_PasswordChanged"
                                     Background="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"
                                     Foreground="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"
                                     BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBorderKey}}"/>
                        <Button Command="{Binding SaveAnthropicKeyCommand}"
                                Content="Save"
                                Padding="8,2"
                                Margin="6,0,0,0"
                                Background="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"
                                Foreground="{DynamicResource {x:Static vs:VsBrushes.ButtonTextKey}}"
                                BorderBrush="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"/>
                    </StackPanel>

                    <!-- Status message -->
                    <TextBlock Text="{Binding SettingsStatus}"
                               Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                               FontStyle="Italic"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Expander>

        <!-- == Analyze button and status == -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" DockPanel.Dock="Top">
            <Button Command="{Binding AnalyzeCommand}"
                    Content="Analyze Assumptions"
                    Padding="12,4"
                    Background="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"
                    Foreground="{DynamicResource {x:Static vs:VsBrushes.ButtonTextKey}}"
                    BorderBrush="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"/>
            <TextBlock Text="Analyzing..."
                       FontStyle="Italic"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                       Visibility="{Binding IsAnalyzing}"/>
        </StackPanel>

        <!-- == Results label == -->
        <TextBlock Text="Results:"
                   FontWeight="Bold"
                   Margin="0,0,0,4"
                   Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                   DockPanel.Dock="Top"/>

        <!-- == Results area fills remaining space == -->
        <TextBox Text="{Binding ResultText, Mode=OneWay}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
                 Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                 BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBorderKey}}"
                 FontFamily="Consolas"/>
    </DockPanel>
</UserControl>
