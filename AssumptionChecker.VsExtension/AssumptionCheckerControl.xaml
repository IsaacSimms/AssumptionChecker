<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml">
    <StackPanel Orientation="Vertical" Margin="10">
        <TextBlock Text="Enter prompt to analyze:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox 
            Text="{Binding PromptText, Mode=TwoWay}" 
            TextWrapping="Wrap" 
            AcceptsReturn="True"
            MinHeight="100"
            Margin="0,0,0,10"
            VerticalScrollBarVisibility="Auto"/>

        <Button Command="{Binding AnalyzeCommand}" Content="Analyze Assumptions" Margin="0,0,0,10"/>

        <TextBlock Text="Analyzing..." FontStyle="Italic" Visibility="{Binding IsAnalyzing}"/>

        <TextBlock Text="Results:" FontWeight="Bold" Margin="0,10,0,5"/>
        <TextBox 
            Text="{Binding ResultText, Mode=OneWay}" 
            IsReadOnly="True"
            TextWrapping="Wrap"
            AcceptsReturn="True"
            MinHeight="200"
            VerticalScrollBarVisibility="Auto"
            FontFamily="Consolas"/>
    </StackPanel>
</DataTemplate>