<UserControl x:Class="AssumptionChecker.VsExtension.AssumptionCheckerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
             Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
             FontFamily="{DynamicResource {x:Static vs:VsFonts.EnvironmentFontFamilyKey}}"
             FontSize="{DynamicResource {x:Static vs:VsFonts.EnvironmentFontSizeKey}}">
    <DockPanel Margin="8">
        <!-- Input area docked at top -->
        <TextBlock Text="Enter prompt to analyze:"
                   FontWeight="Bold"
                   Margin="0,0,0,4"
                   Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                   DockPanel.Dock="Top"/>
        <TextBox Text="{Binding PromptText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MinHeight="80"
                 MaxHeight="150"
                 Margin="0,0,0,8"
                 Background="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBackgroundKey}}"
                 Foreground="{DynamicResource {x:Static vs:VsBrushes.WindowTextKey}}"
                 BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ComboBoxBorderKey}}"
                 VerticalScrollBarVisibility="Auto"
                 DockPanel.Dock="Top"/>

        <!-- Button and status docked at top -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" DockPanel.Dock="Top">
            <Button Command="{Binding AnalyzeCommand}"
                    Content="Analyze Assumptions"
                    Padding="12,4"
                    Background="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"
                    Foreground="{DynamicResource {x:Static vs:VsBrushes.ButtonTextKey}}"
                    BorderBrush="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"/>
            <TextBlock Text="Analyzing..."
                       FontStyle="Italic"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                       Visibility="{Binding IsAnalyzing}"/>
        </StackPanel>

        <!-- Results label docked at top -->
        <TextBlock Text="Results:"
                   FontWeight="Bold"
                   Margin="0,0,0,4"
                   Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                   DockPanel.Dock="Top"/>

        <!-- Results area fills remaining space -->
        <TextBox Text="{Binding ResultText, Mode=OneWay}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}"
                 Foreground="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}"
                 BorderBrush="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBorderKey}}"
                 FontFamily="Consolas"/>
    </DockPanel>
</UserControl>
